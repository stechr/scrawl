# Scrawl

A macOS screen-annotation overlay you can draw on top of any app — with two front doors (a human toolbar and an agent control API/MCP) over one shared core.

_Initial commit; full source lands via the promote PR._
